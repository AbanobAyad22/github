name: print hello

on:
    push:
        branches:
            - master

jobs:
    ci:   
       runs-on: ubuntu-latest
       steps:
         - name: print hello
           run: echo "hello,from the first workflow"
         - name: print goodbye  
           run: echo "goodbye"
          
    cd:
        runs-on: ubuntu-latest
        steps:
            - name: ls
              run: ls -la     
