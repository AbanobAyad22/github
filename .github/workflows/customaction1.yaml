name: custom action 1 
  
on:
    push:
        branches: ["master"]

jobs:
    custom1:
        runs-on: ubuntu-latest
        steps: 
            - name: use custom action
              uses: ./.github/actions/cat