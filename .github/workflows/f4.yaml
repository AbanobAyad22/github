on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - poduction

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Show input
        run: echo "Deploying to ${{ inputs.environment }}"
