name: show issue content

on:
  issues: 
    types:  opened


jobs:
    reserved-wprds:
        runs-on: ubuntu-latest
        steps:
            - name: git clone
              uses: actions/checkout@v6
            
            - name: show issue content
              run: echo "${{ toJson(github.event) }}"
