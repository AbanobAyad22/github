name: this workflow is to print hello

on:
  push: 
    branches: ['main']


jobs:
    say_hello: 
        runs-on: ubuntu-latest
        steps:
            - name: say hello 
              run: echo "hello ya abanoub"