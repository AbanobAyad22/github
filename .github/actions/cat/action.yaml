name: cat file
description: show the content of file
inputs:
  path:
     description: "enter you input to cat"
     required: true
     default: "hello"


runs:
  using: "composite"
  steps: 
    - name: cat file
      shell: bash
      run: |
         cat ${{ inputs.path }}
         