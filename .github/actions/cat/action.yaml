name: cat file
description: hhh
inputs:
  path:
    description: file to show what inside
    required: true
    default: "note.txt"

runs:
  using: "composite" 
  steps: 
    - name: cat file
      shell: bash
      run: |
        echo "note.txt"
        cat note.txt